export interface ApplicationContactDetail {
  application_id: number;
  contact_id: number;
  contact_name: string;
  contact_type: string;
  contact_phone: string;
  contact_email: string;
  active_sw: boolean;
}
